<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Trip Dashboard</title>
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: linear-gradient(to bottom right, #eaf1ff, #ffffff);
      margin: 0;
      padding: 0;
      color: #333;
      display: flex;
    }

    .sidebar {
      width: 220px;
      background: #1e40af;
      color: white;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px 10px;
      box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
      position: fixed;
      left: 0;
      top: 0;
    }

    .sidebar h2 {
      margin-bottom: 40px;
      font-size: 22px;
      color: #ffffff;
    }

    .nav-link {
      width: 100%;
      text-align: left;
      padding: 10px 20px;
      margin-bottom: 10px;
      font-weight: 500;
      color: #e0e7ff;
      border-radius: 8px;
      display: flex;
      align-items: center;
      gap: 10px;
      transition: background-color 0.3s;
      cursor: pointer;
    }

    .nav-link:hover,
    .nav-link.active {
      background-color: #3b82f6;
      color: #fff;
    }

    .nav-link span {
      font-size: 16px;
    }

    .nav-footer {
      margin-top: auto;
      font-size: 13px;
      color: #c7d2fe;
    }

    .container {
      margin-left: 240px;
      max-width: 900px;
      flex: 1;
      margin-top: 40px;
      margin-bottom: 40px;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      padding: 30px;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
    }

    .header h1 {
      color: #1e40af;
      font-size: 26px;
      margin: 0;
    }

    .back-btn {
      background-color: #1e40af;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out;
    }

    .back-btn:hover {
      background-color: #1d4ed8;
    }

    .dashboard {
      display: flex;
      gap: 20px;
    }

    .event-box {
      flex: 1;
      background: #f9fafb;
      border-radius: 12px;
      padding: 20px;
      border: 1px solid #e5e7eb;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .event-box h3 {
      color: #1e40af;
      margin-top: 0;
      margin-bottom: 15px;
    }

    .event-list {
      list-style: none;
      padding-left: 0;
      margin-bottom: 15px;
      overflow-y: auto;
      max-height: 160px;
    }

    .event-list li {
      background: #e0e7ff;
      padding: 8px;
      border-radius: 6px;
      margin-bottom: 8px;
      text-align: center;
      color: #1e3a8a;
      font-weight: 500;
    }

    .add-btn {
      background-color: #22c55e;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 10px;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out;
    }

    .add-btn:hover {
      background-color: #16a34a;
    }

    .right-section {
      flex: 1.2;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .info-box {
      background: #f9fafb;
      border-radius: 12px;
      padding: 20px;
      border: 1px solid #e5e7eb;
      transition: transform 0.2s;
    }

    .info-box:hover {
      transform: translateY(-3px);
    }

    .info-box strong {
      color: #1e40af;
    }

    .progress-bar {
      background: #e5e7eb;
      border-radius: 8px;
      overflow: hidden;
      height: 10px;
      margin-top: 10px;
    }

    .progress {
      height: 10px;
      background: linear-gradient(to right, #22c55e, #86efac);
      width: 70%;
    }

    /* Modal */
    .modal {
      display: none;
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.4);
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.2);
      width: 300px;
      text-align: center;
    }

    .modal-content h3 {
      margin-top: 0;
      color: #1e40af;
    }

    .modal-content input {
      width: 100%;
      margin-top: 10px;
      padding: 8px;
      border: 1px solid #d1d5db;
      border-radius: 8px;
    }

    .modal-content button {
      margin-top: 10px;
      background: #22c55e;
      border: none;
      color: white;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      transition: background-color 0.2s;
    }

    .modal-content button:hover {
      background: #16a34a;
    }
  </style>
</head>
<body>
  <!-- Sidebar -->
  <div class="sidebar">
    <h2>🌍 Trips</h2>
    <div class="nav-link active"><span>🏠</span> Home</div>
    <div class="nav-link"><span>🧳</span> Trips</div>
    <div class="nav-link"><span>💰</span> Budget</div>
    <div class="nav-link"><span>📅</span> Events</div>
    <div class="nav-link"><span>👥</span> People</div>
    <div class="nav-link"><span>⚙️</span> Settings</div>

    <div class="nav-footer">© 2025 Aroha Careers</div>
  </div>

  <div class="container">
    <div class="header">
      <h1>Welcome, Username 🌴</h1>
      <button class="back-btn">Back</button>
    </div>

    <div class="dashboard">
      <!-- Left: Events -->
      <div class="event-box">
        <h3>Events</h3>
        <ul class="event-list" id="eventList">
          <li>Event 1</li>
          <li>Event 2</li>
        </ul>
        <button class="add-btn" id="addEventBtn">+ Add Event</button>
      </div>

      <div class="right-section">
        <div class="info-box">
          <strong>Trips</strong><br>
          Destination: #1 <br>
          Dates: start – end
        </div>

        <div class="info-box">
          <strong>Budget</strong><br>
          Used: $$$ / $$
          <div class="progress-bar">
            <div class="progress"></div>
          </div>
        </div>

        <div class="info-box">
          <strong>People</strong><br>
          👤 You<br>
          👤 #2<br>
          👤 #3<br>
          👤 #4
        </div>
      </div>
    </div>
  </div>

  <!-- Modal -->
  <div class="modal" id="eventModal">
    <div class="modal-content">
      <h3>Add New Event</h3>
      <input type="text" id="eventName" placeholder="Enter event name" />
      <button id="saveEventBtn">Save</button>
    </div>
  </div>

  <script>
    const addEventBtn = document.getElementById('addEventBtn');
    const eventModal = document.getElementById('eventModal');
    const saveEventBtn = document.getElementById('saveEventBtn');
    const eventList = document.getElementById('eventList');
    const eventNameInput = document.getElementById('eventName');

    addEventBtn.onclick = () => {
      eventModal.style.display = 'flex';
      eventNameInput.value = '';
      eventNameInput.focus();
    };

    saveEventBtn.onclick = () => {
      const name = eventNameInput.value.trim();
      if (name) {
        const li = document.createElement('li');
        li.textContent = name;
        eventList.appendChild(li);
        eventModal.style.display = 'none';
      }
    };

    window.onclick = (e) => {
      if (e.target === eventModal) {
        eventModal.style.display = 'none';
      }
    };
  </script>
</body>
</html>




