<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Travel App Dashboard</title>
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
  />
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: linear-gradient(to bottom right, #eaf1ff, #ffffff);
      margin: 0;
      padding: 0;
      color: #333;
      display: flex;
    }

    .sidebar {
      width: 240px;
      background: #1e40af;
      color: white;
      height: 100vh;
      display: flex;
      flex-direction: column;
      padding: 25px 15px;
      position: fixed;
      top: 0;
      left: 0;
      box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
    }

    .sidebar-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 40px;
    }

    .sidebar-header h2 {
      font-size: 20px;
      font-weight: 700;
      color: #ffffff;
      letter-spacing: 0.5px;
    }

    .logo-icon {
      font-size: 22px;
      color: #ffffff;
    }

    .nav {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .nav-item {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
      color: #e0e7ff;
      font-weight: 500;
      font-size: 15px;
      padding: 10px 15px;
      border-radius: 8px;
      transition: background 0.3s, color 0.3s;
    }

    .nav-item:hover {
      background: #3b82f6;
      color: #fff;
    }

    .nav-item.active {
      background: #3b82f6;
      color: #fff;
      font-weight: 600;
    }

    .nav-item i {
      font-size: 16px;
    }

    .nav-footer {
      margin-top: auto;
      font-size: 13px;
      color: #c7d2fe;
      text-align: center;
      padding-top: 20px;
    }

    /* ===== Main Container ===== */
    .container {
      margin-left: 260px;
      max-width: 900px;
      flex: 1;
      margin-top: 40px;
      margin-bottom: 40px;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      padding: 30px;
      transition: margin-left 0.3s ease-in-out;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
    }

    .header h1 {
      color: #1e40af;
      font-size: 26px;
      margin: 0;
    }

    .back-btn {
      background-color: #1e40af;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out;
    }

    .back-btn:hover {
      background-color: #1d4ed8;
    }

    .dashboard {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
    }

    .event-box {
      flex: 1;
      background: #f9fafb;
      border-radius: 12px;
      padding: 20px;
      border: 1px solid #e5e7eb;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      min-width: 280px;
    }

    .event-box h3 {
      color: #1e40af;
      margin-top: 0;
      margin-bottom: 15px;
    }

    .event-list {
      list-style: none;
      padding-left: 0;
      margin-bottom: 15px;
      overflow-y: auto;
      max-height: 160px;
    }

    .event-list li {
      background: #e0e7ff;
      padding: 8px;
      border-radius: 6px;
      margin-bottom: 8px;
      text-align: center;
      color: #1e3a8a;
      font-weight: 500;
    }

    .add-btn {
      background-color: #22c55e;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 10px;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out;
    }

    .add-btn:hover {
      background-color: #16a34a;
    }

    .right-section {
      flex: 1.2;
      display: flex;
      flex-direction: column;
      gap: 20px;
      min-width: 260px;
    }

    .info-box {
      background: #f9fafb;
      border-radius: 12px;
      padding: 20px;
      border: 1px solid #e5e7eb;
      transition: transform 0.2s;
    }

    .info-box:hover {
      transform: translateY(-3px);
    }

    .info-box strong {
      color: #1e40af;
    }

    .progress-bar {
      background: #e5e7eb;
      border-radius: 8px;
      overflow: hidden;
      height: 10px;
      margin-top: 10px;
    }

    .progress {
      height: 10px;
      background: linear-gradient(to right, #22c55e, #86efac);
      width: 70%;
    }

    .modal {
      display: none;
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.4);
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.2);
      width: 300px;
      text-align: center;
    }

    .modal-content h3 {
      margin-top: 0;
      color: #1e40af;
    }

    .modal-content input {
      width: 100%;
      margin-top: 10px;
      padding: 8px;
      border: 1px solid #d1d5db;
      border-radius: 8px;
    }

    .modal-content button {
      margin-top: 10px;
      background: #22c55e;
      border: none;
      color: white;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      transition: background-color 0.2s;
    }

    .modal-content button:hover {
      background: #16a34a;
    }
  </style>
</head>
<body>

  <!-- Sidebar -->
  <div class="sidebar">
    <div class="sidebar-header">
      <i class="fa-solid fa-plane-departure logo-icon"></i>
      <h2>TRAVEL APP</h2>
    </div>

    <nav class="nav">
      <a href="#" class="nav-item active"><i class="fa-solid fa-gauge"></i><span>Dashboard</span></a>
      <a href="#" class="nav-item"><i class="fa-solid fa-suitcase"></i><span>My Trips</span></a>
      <a href="#" class="nav-item"><i class="fa-solid fa-wallet"></i><span>Budget</span></a>
      <a href="#" class="nav-item"><i class="fa-solid fa-calendar-day"></i><span>Events</span></a>
      <a href="#" class="nav-item"><i class="fa-solid fa-user-group"></i><span>People</span></a>
      <a href="#" class="nav-item"><i class="fa-solid fa-gear"></i><span>Settings</span></a>
    </nav>

    <div class="nav-footer">© 2025 Aroha Careers</div>
  </div>

  <!-- Main Content -->
  <div class="container">
    <div class="header">
      <h1>Welcome, Username 🌴</h1>
      <button class="back-btn">Back</button>
    </div>

    <div class="dashboard">
      <!-- Events -->
      <div class="event-box">
        <h3>Events</h3>
        <ul class="event-list" id="eventList">
          <li>Event 1</li>
          <li>Event 2</li>
        </ul>
        <button class="add-btn" id="addEventBtn">+ Add Event</button>
      </div>

      <!-- Right Section -->
      <div class="right-section">
        <div class="info-box">
          <strong>Trips</strong><br>
          Destination: #1 <br>
          Dates: start – end
        </div>

        <div class="info-box">
          <strong>Budget</strong><br>
          Used: $$$ / $$
          <div class="progress-bar"><div class="progress"></div></div>
        </div>

        <div class="info-box">
          <strong>People</strong><br>
          👤 You<br>
          👤 #2<br>
          👤 #3<br>
          👤 #4
        </div>
      </div>
    </div>
  </div>

  <!-- Modal -->
  <div class="modal" id="eventModal">
    <div class="modal-content">
      <h3>Add New Event</h3>
      <input type="text" id="eventName" placeholder="Enter event name" />
      <button id="saveEventBtn">Save</button>
    </div>
  </div>

  <script>
    const addEventBtn = document.getElementById('addEventBtn');
    const eventModal = document.getElementById('eventModal');
    const saveEventBtn = document.getElementById('saveEventBtn');
    const eventList = document.getElementById('eventList');
    const eventNameInput = document.getElementById('eventName');

    addEventBtn.onclick = () => {
      eventModal.style.display = 'flex';
      eventNameInput.value = '';
      eventNameInput.focus();
    };

    saveEventBtn.onclick = () => {
      const name = eventNameInput.value.trim();
      if (name) {
        const li = document.createElement('li');
        li.textContent = name;
        eventList.appendChild(li);
        eventModal.style.display = 'none';
      }
    };

    window.onclick = (e) => {
      if (e.target === eventModal) eventModal.style.display = 'none';
    };
  </script>
</body>
</html>
